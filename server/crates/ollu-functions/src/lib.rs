// Placeholder for serverless functions (e.g. LLM proxy with user's API key).
// Reserved for future implementation; see project notes for scope.

use axum::Router;

pub fn functions_router() -> Router {
    Router::new()
}
