# Copy to /opt/ollu/.env on the VPS and fill in. Operator-managed, never committed.
# Comma-separated Google OAuth client_id(s) used to validate id_token audience.
GOOGLE_AUDIENCES=your-client-id.apps.googleusercontent.com
# Log level / filter (tracing EnvFilter syntax).
RUST_LOG=info
